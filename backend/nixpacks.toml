[phases.setup]

[phases.install]
cmd = "pip install -r requirements.txt"

[phases.build]
cmd = "echo 'No build step required for backend'"

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
